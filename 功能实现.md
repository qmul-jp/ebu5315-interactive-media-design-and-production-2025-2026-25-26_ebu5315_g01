# 成就与皮肤功能 UI 页面及入口实现规划

为保持与现有游戏页面（玻璃拟态、暗色主题、现代感扁平化设计）的整体风格契合，我们做如下规划：

## 1. 游戏入口调整规划
在现有的 `sniperStartOverlay` (开始弹窗) 中增加成就、皮肤以及记录的入口。
- **UI设计**：在“开始 (Start)”按钮下方，添加一行三个并排的次级按钮（使用现有的 `btn btn-outline` 样式），分别对应“🏆 成就”、“🎨 皮肤”和“📊 记录”。
- **交互**：点击对应按钮，隐藏当前的开始面板，显示对应的“成就展示页”、“皮肤选择页”或“记录数据页”面板。面板内顶部带有“返回”按钮以退回到开始界面。

## 2. 成就功能升级与成就页面规划
**功能改造**：
- 修改 `pi-sniper.js` 中的解锁逻辑，根据触发频率不同分为：`once`（单次，不可重复）、`once_per_game`（每局一次）、`repeatable`（每局多次）。
- 成就的数据结构升级为记录解锁次数 `{ unlocked: true, count: N }`。仅对 `once_per_game` 和 `repeatable` 的成就累加完成次数。

**成就页面设计**：
- **整体布局**：居中的弹窗面板（限制最大高度，内部可滚动）。
- **内容呈现**：
  1. **成就名称**：读取多语言文件中的成就名称。
  2. **图标**：根据成就类型配置对应的 Emoji 图标（如 🎯, 👑, 🔥 等）。
  3. **达成描述**：显示成就的解锁条件说明。
  4. **完成次数**：对于允许多次完成的成就，卡片右侧以徽章形式高亮显示 `xN`（N为完成次数）；对于总共只能完成1次的成就，则隐藏次数角标或显示“已达成”。
  5. **未解锁状态**：未获得的成就卡片整体降低透明度，次数显示为锁定图标 🔒。

## 3. 皮肤页面规划
**皮肤页面设计**：
- **整体布局**：面板顶部设置横向的分类 Tab（准星、颜色、单位圆、粒子、背景），下方展示该类别下的皮肤网格列表。
- **内容呈现（单个皮肤卡片）**：
  1. **皮肤名称**：多语言支持的皮肤名称。
  2. **皮肤样式**：卡片内包含一个微型预览区（如 CSS 绘制的颜色块或小图标）来示意皮肤效果。
  3. **皮肤解锁方式**：若未解锁，卡片遮罩显示解锁条件（如“累计10次完美射击解锁”）。
  4. **皮肤介绍**：简短的悬浮提示或卡片副标题。
  5. **穿戴按钮**：已解锁的皮肤卡片底部显示“穿戴”按钮；当前正在使用的皮肤按钮高亮显示“已装备”，并不可点击。

## 4. 记录页面规划
**数据记录功能**：
- 在 `pi-sniper.js` 中扩充 `updateLifetimeStats` 函数，增加对游玩时间、击中/未击中次数等详细数据的累加。
- 记录的数据维度包括：
  1. 总游玩局数 (局)
  2. 总游玩时间 (显示为 秒/分/时)
  3. 总击中目标次数 (包含 perfect/excellent/good/hit)
  4. 完美射击次数 (perfect)
  5. 脱靶次数 (miss)
  6. 历史最大连击数 (maxCombo)
  7. 击败 Boss 数量
  8. 触发 Frenzy 状态次数

**记录页面设计**：
- **整体布局**：与成就页面相似的弹窗面板，展示各项数据的统计网格。
- **内容呈现**：
  - 使用两列或三列的统计卡片布局，每项数据包含对应的 Icon、数据标签（如“历史最大连击”）和醒目的数字。

## 5. 后续开发实施步骤 (待您同意后逐步执行)
1. **步骤一**：更新 `pi-sniper.js` 中成就的计数逻辑及增加对游玩时间、射击详情的持久化记录。
2. **步骤二**：在 `game.html` 的开始弹窗中加入成就、皮肤和记录的入口按钮，并增加三个新页面的 HTML 骨架。
3. **步骤三**：在 `pi-sniper.css` 中添加成就卡片、皮肤网格和数据统计网格的专属样式。
4. **步骤四**：在 `pi-sniper.js` 中补充界面的渲染逻辑与交互事件（页面切换、渲染成就列表、渲染皮肤列表、渲染记录数据、穿戴皮肤功能）。
