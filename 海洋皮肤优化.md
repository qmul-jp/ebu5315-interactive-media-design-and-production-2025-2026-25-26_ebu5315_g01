# 海洋皮肤优化方案 (Ocean Skin Optimization)

根据您的要求，我将对 `pi-sniper` 游戏中的海洋皮肤 (`ocean`) 进行全面升级，增加丰富的海底生态和动态生物。以下是具体的设计与技术实现方案：

## 一、 海底环境渲染 (Seabed Environment)

### 1. 拟真起伏沙滩
*   **设计**：不再使用平坦的直线作为屏幕底部，而是利用 `Canvas` 的贝塞尔曲线 (`bezierCurveTo`) 结合正弦波，绘制出具有高低起伏的沙丘地形。
*   **颜色**：使用线性渐变（浅沙黄色到深褐色），并在沙滩表面叠加半透明的细小颗粒（噪点）来模拟沙子的质感。

### 2. 错落生长的海草
*   **设计**：改变海草全屏随机分布的现状。海草将聚集成丛（例如左侧一丛、右侧一丛），并且其根部（y坐标）会精确贴合起伏沙滩的曲线。
*   **动画**：保留随水流摇摆的动画，但动作会更加柔和自然。

### 3. 多彩珊瑚礁
*   **设计**：在沙滩上远离中心单位圆的位置（例如屏幕最左下角或最右下角），绘制数丛色彩鲜艳的珊瑚。
*   **样式**：包含管状珊瑚（使用多次分支的曲线绘制，颜色为亮粉、荧光橙）和脑状珊瑚（带有多孔纹理的半圆形，颜色为蓝绿色）。

---

## 二、 动态海洋生物 (Dynamic Creatures)

### 1. 巡逻的螃蟹 (Crabs)
*   **数量**：2 只。
*   **行为**：在沙滩上特定的区间内左右来回横移。
*   **动画机制**：
    *   **位移**：利用 `Math.sin(time * speed)` 控制水平X轴的往复运动。
    *   **动作**：使用三角函数控制螃蟹两侧步足的旋转角度，实现交替抬起和放下的逼真爬行效果；大钳子也会随着身体微微上下摆动。
*   **样式**：亮红色甲壳，带有光泽质感。

### 2. 穿梭的大鱼 (Large Fishes)
*   **数量**：2 只。
*   **尺寸**：视觉面积约为单位圆面积的 1/4 左右。
*   **行为**：从屏幕左侧向右侧游动，且两只鱼的入水高度和出水高度不同，形成交叉或平行的斜向游动路径，并会**穿过中心的单位圆区域**。
*   **样式对比**：
    *   **鱼 A (优雅型)**：修长的流线型身躯，颜色为深海蓝渐变至蓝绿色。尾鳍细长。
    *   **鱼 B (宽胖型)**：略显圆润的体态，颜色为橙黄色渐变带有白色条纹（类似小丑鱼或神仙鱼的温和版）。尾鳍宽大。
    *   *注：颜色饱和度会适中，采用半透明光影，确保不会喧宾夺主干扰玩家视线。*
*   **动画机制**：身体会有轻微的上下浮动，尾鳍利用 `Math.sin(time)` 产生左右摆动的物理动画。

### 3. 漂游的水母群 (Jellyfish Cluster)
*   **参考**：《海绵宝宝》中的经典水母形象（粉色半透明伞状身体，带有深紫色斑点，细长的波浪触须）。
*   **数量**：3-5 只聚集在屏幕某一侧的水中。
*   **行为**：整体沿着倾斜 10 度的垂直线上下游弋，移动总路程极短（约为单位圆半径的 1/8），呈现出悬浮在水中的状态。
*   **动画机制**：
    *   **呼吸感**：水母的伞状半球体会随着时间周期性地收缩和膨胀（利用 `scale` 结合 `Math.sin`）。
    *   **触须游动**：下方的触须使用多段贝塞尔曲线绘制，控制点坐标随时间产生波浪状偏移，模拟在水中轻柔划动的姿态。

---

## 三、 技术执行步骤

1.  **更新状态初始化**：在 `pi-sniper.js` 的 `initBgAnimState` 中，为螃蟹、大鱼、水母和沙滩起伏地形生成初始数据（位置、大小、速度区间、颜色参数）。
2.  **分层绘制渲染**：修改 `drawBackgroundDecorations` 中的 `ocean` 分支，按照严格的层级顺序绘制：
    *   **底层**：深海背景色。
    *   **中下层**：穿梭的大鱼（在极远处，但颜色清晰）、水母群。
    *   **中层**：起伏的沙滩、珊瑚丛、海草丛。
    *   **前景**：在沙滩上爬行的螃蟹。
3.  **动画函数封装**：为鱼类、螃蟹、水母分别封装独立的绘制函数（如 `drawFish`, `drawCrab`, `drawJellyfish`），以保持代码整洁和 Canvas 状态 (`ctx.save/restore`) 的安全隔离。

---
请您验收此方案。如果您对其中任何元素（如颜色、大小、动画方式）感到满意，请回复确认，我将立即开始编写代码实现！