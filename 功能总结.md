# 圆周狙击 (Pi Sniper) 成就与皮肤功能总结

## 1. 现有成就系统功能
目前游戏已经内置了成就追踪和解锁逻辑，所有的成就达成条件在 `ACHIEVEMENT_DEFS` 中定义。成就数据目前保存在 `localStorage` 中（键名为 `pi_sniper_achievements`）。

**现有成就列表：**
- **firstPerfect**: 首次完美射击（总实现次数1次）
- **sniper**: 累计10次完美射击（总实现次数1次）
- **godlike**: 累计20次完美射击（总实现次数1次）
- **zeroError**: 连续10次完美射击（每局可触发多次，记录总完成次数）
- **combo5 / combo15 / combo30**: 达到5/15/30次连击（总实现次数1次）
- **bossHunter / bossHarvester**: 击败1次Boss / 击败10次Boss（总实现次数1次）
- **timeMaster**: 最后一秒完美射击（每局限触发1次，记录总完成次数）
- **comeback**: 连击断掉后重新达到10连击（总实现次数1次）
- **frenzyRegular**: 单局触发3次Frenzy状态（总实现次数1次）

**当前局限性**：目前的成就系统虽然已经按照频率分类，但缺乏用户可见的成就展示页面入口。

## 2. 现有皮肤系统功能
游戏支持通过成就或统计数据解锁不同的皮肤样式，配置定义在 `SKIN_UNLOCK_CONDITIONS` 中。皮肤配置当前保存在 `localStorage` 的 `pi_sniper_skin` 中。

**现有皮肤类别与样式：**
- **准星 (crosshair)**: 默认 (default), 圆形 (circle), 激光 (laser), 弓形 (bow)
- **瞄准线颜色 (lineColor)**: 绿色 (green), 金色 (gold), 蓝色 (blue), 紫色 (purple), 彩虹 (rainbow)
- **单位圆样式 (circleStyle)**: 标准 (standard), 霓虹 (neon), 极简 (minimal), 复古 (retro)
- **粒子效果 (particle)**: 标准 (standard), 星光 (star), 火焰 (fire), 电弧 (electric)
- **背景主题 (background)**: 深空 (deepSpace), 海洋 (ocean), 森林 (forest), 赛博朋克 (cyberpunk)

**当前局限性**：皮肤系统已有完整的视觉渲染支持和解锁逻辑，但目前没有提供供玩家自由查看、预览和切换（穿戴）皮肤的UI页面入口。
