# 背景音乐 (BGM) 功能可行性研究与执行方案

## 1. 当前音频系统现状分析
通过检查代码库，当前项目的音频系统由 `game-audio.js` 统一管理。
- **技术栈**：完全使用了 **Web Audio API** 进行程序化音频合成（通过 Oscillator 振荡器生成音效），**没有引入任何外部的 `.mp3` 或 `.wav` 音频文件**。
- **现有功能**：目前 `GameAudio` 类中仅包含各种短促的音效（SFX），如点击音 `playClick`、射击音 `playShoot`、连击音 `playCombo` 等。
- **局限性**：由于当前完全依赖代码实时合成声音，虽然可以合成出简单的 8-bit 风格旋律，但如果要实现**好听、有氛围感且时长较长的背景音乐**，通过代码编写音符序列（Tracker 模式）将极其复杂且效果有限。

## 2. 可行性结论
**完全可行**，但为了保证背景音乐的质量和多皮肤主题的契合度，**强烈建议引入外部音频文件（如 `.mp3`）作为背景音乐**，而不是继续使用 Web Audio API 的振荡器去硬敲代码合成。

- **浏览器策略限制**：现代浏览器（Chrome/Safari）禁止页面在用户没有进行任何交互（点击/触摸）之前自动播放声音（Autoplay Policy）。
  - **解决方案**：在《圆周狙击》中，玩家需要点击“开始 (Start)”按钮才会进入游戏。我们可以将“播放 BGM”的触发点绑定在这个“开始”按钮的点击事件上，这样就能完美绕过浏览器的自动播放限制。

## 3. 详细执行方案

### 步骤一：准备音频资源
1. 需要准备 4 首无版权限制的、可循环播放的背景音乐文件（建议为压缩率较高的 `.mp3` 格式，单首大小控制在 1MB 左右）：
   - `bgm_deepSpace.mp3` (深空主题：空灵、电子合成器)
   - `bgm_ocean.mp3` (海洋主题：舒缓、水下环境音)
   - `bgm_forest.mp3` (森林主题：自然、鸟鸣、木吉他)
   - `bgm_cyberpunk.mp3` (赛博朋克主题：强节奏、Synthwave)
2. 在项目根目录下新建一个 `assets/audio/` 文件夹，将这些音频文件放入其中。

### 步骤二：扩展 `game-audio.js`
修改现有的全局音频管理器，加入对 HTML5 `Audio` 对象（或基于 Web Audio API 的 BufferSource）的支持。
1. 新增 BGM 实例管理：
   ```javascript
   let currentBgm = null;
   const bgmList = {
       deepSpace: new Audio('assets/audio/bgm_deepSpace.mp3'),
       ocean: new Audio('assets/audio/bgm_ocean.mp3'),
       forest: new Audio('assets/audio/bgm_forest.mp3'),
       cyberpunk: new Audio('assets/audio/bgm_cyberpunk.mp3')
   };
   // 统一设置循环播放
   Object.values(bgmList).forEach(audio => audio.loop = true);
   ```
2. 暴露 `playBgm(skinKey)` 和 `stopBgm()` 方法：
   - 在 `playBgm` 中，首先判断全局音效开关是否打开。
   - 如果当前有正在播放的 BGM，先将其 `pause()` 并重置 `currentTime = 0`。
   - 根据传入的 `skinKey` 找到对应的音频对象，调用 `play()`。
   - 需要处理音量同步（背景音乐音量通常应低于音效，如设置为全局音量的 50%）。

### 步骤三：在 `pi-sniper.js` 中接入逻辑
1. **游戏开始时播放**：
   在 `startGame()` 函数内部，调用 `GameAudio.playBgm(skin.background)`。此时用户已经发生了点击交互，BGM 会顺利播放。
2. **游戏结束时停止**：
   在 `endGame()` 函数内部，调用 `GameAudio.stopBgm()`，或者如果希望结算界面也保持音乐，可以在返回主菜单时停止。
3. **切换皮肤时无缝切换**：
   在 `equipSkin(category, key)` 函数中，如果用户修改了 `background` 分类，并且当前处于游戏进行状态（或者你在主界面也想播放），则需要调用 `GameAudio.playBgm(key)` 进行无缝切换。

### 步骤四：UI 与用户控制（可选但推荐）
为了用户体验，建议在游戏内或统一的设置面板中，将“背景音乐音量”和“音效音量”拆分开来，或者至少保证现有的全局静音按钮能够同时控制 BGM 的暂停和恢复。
