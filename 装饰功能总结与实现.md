# 背景皮肤装饰功能总结与实现规划

## 1. 需求背景与功能总结
目前《圆周狙击》游戏内的背景皮肤（深空、海洋、森林、赛博朋克）仅通过改变径向渐变的颜色来实现，视觉表现较为单调。
本次优化的目标是为这四个背景增加专属的**动态与静态视觉元素**，从而大幅提升游戏的沉浸感和各皮肤的辨识度。

具体规划如下：
- **深空 (Deep Space)**：加入多层视差移动的星空背景，以及一颗带有光晕的静谧星球。
- **海洋 (Ocean)**：底部加入随水流（正弦波）摇曳的海草，背景中有随机游动的简单鱼群剪影和上升的气泡。
- **森林 (Forest)**：底部加入错落有致的树林剪影和一座透出微光的林中小屋，空中可飘浮少许动态的萤火虫。
- **赛博朋克 (Cyberpunk)**：底部及两侧加入带有霓虹色块（窗户/广告牌）的高楼大厦剪影，空中加入缓慢移动的霓虹光带或飞行器轨迹。

## 2. 技术可行性验证
**结论：完全可行且性能可控。**
《圆周狙击》游戏本身基于原生 HTML5 Canvas 进行渲染，因此我们完全可以利用 Canvas 的 2D 绘图 API 绘制这些效果，无需引入庞大的图片或视频资源。

*   **性能保证**：我们采用纯几何图形（路径、圆、矩形）进行实时绘制，能够保持极高的加载速度。对于动态粒子（星星、鱼、气泡、萤火虫），只要将数量控制在合理范围内（例如每个场景 30-50 个动态元素），绝不会对每秒 60 帧的渲染造成压力。
*   **动画驱动**：游戏主循环中已有一个持续运行的 `requestAnimationFrame`。我们可以引入一个全局的时间戳 `timer`，将其传入背景渲染函数中，利用 `Math.sin(timer)` 和 `timer * speed` 来实现水草的正弦波摇曳和鱼群的线性位移等平滑的动态效果。
*   **自适应屏幕**：所有绘制的坐标将基于画布的实时宽高 (`W`, `H`) 进行相对计算，确保在不同窗口尺寸和详情预览面板的小 Canvas 中都能自适应、正确地显示。

## 3. 具体实现步骤规划
1. **状态管理初始化**：
   在 `pi-sniper.js` 中新增一个全局对象 `bgAnimState`，用于存储各个场景的动态粒子状态（如星星坐标、鱼群坐标、萤火虫位置、气泡等）。
   
2. **定义背景装饰渲染核心函数**：
   在 `pi-sniper.js` 中新增一个独立的函数 `drawBackgroundDecorations(ctx, width, height, skinKey, time)`。
   
3. **在游戏主循环中接入渲染**：
   在主游戏循环 `draw()` 中，以及皮肤详情弹窗的 `startSkinPreview()` 循环中，在绘制完背景的纯色渐变后、绘制准星与单位圆之前，调用 `drawBackgroundDecorations()` 函数。
   
4. **实现各主题的具体绘制逻辑 (纯 Canvas 绘图)**：
   - **深空**：利用随机生成的数组绘制缓慢向下移动的白色小圆点（星星），并在画面右上角利用 `arc` 和 `createRadialGradient` 绘制一颗带有光晕的星球。
   - **海洋**：底部使用 `Math.sin(time + x)` 结合 `quadraticCurveTo` 绘制半透明绿色的海草曲线；更新和绘制横向移动的鱼形多边形和上升的半透明圆圈（气泡）。
   - **森林**：使用多边形路径 `lineTo` 绘制松树冠和木屋轮廓，窗户用亮黄色填充发光；使用随机浮动且透明度闪烁的小黄点代表萤火虫。
   - **赛博朋克**：绘制一系列参差不齐的深色矩形作为楼体，随机在楼体上绘制亮色（粉、青、黄）的小矩形作为霓虹窗户；加入横向快速划过的光线（飞行器轨迹）。

以上方案既能实现你要求的华丽动态特效，又能保证游戏的纯代码轻量化和高性能。