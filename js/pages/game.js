/**
 * 游戏页（占位，可后续接入 canvas / 关卡逻辑）
 */
(function () {
    const startBtn = document.getElementById('gameStartBtn');
    if (startBtn) {
        startBtn.addEventListener('click', () => {
            alert('Game levels coming soon! This will start the interactive circle theorem challenges.');
        });
    }
})();
